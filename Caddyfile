xontrol.co.uk, www.xontrol.co.uk {
    reverse_proxy web:8000

    handle /static/* {
        root * /app
        file_server
    }

    handle /media/* {
        root * /app
        file_server
    }

    encode gzip
}

